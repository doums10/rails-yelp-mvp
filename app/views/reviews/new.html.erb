<h1>Create a new review for
  <%= @restaurant.name %>
</h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
<div class="form-actions">
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.button :submit, class: "btn btn-primary" %>
</div>
<% end %>
<%= link_to 'Back', restaurants_path %>